# Git Workflow

The Git rules are defined by workflow step in agent files:

* Start work on a new version: `.opencode/agents/release-starter.md`
* Commit intermediate work: `.opencode/agents/release-committer.md`
* Build a release and prepare it for publication: `.opencode/agents/release-finisher.md`
